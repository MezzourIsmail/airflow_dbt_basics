name: 'my_duckdb_project'
version: '1.0'
config-version: 2

profile: 'default'

model-paths: ['models']
analysis-paths: ['analysis']
test-paths: ['tests']
seed-paths: ['data']
macro-paths: ['macros']

target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_modules'

seeds:
  my_duckdb_project:
    +schema: raw_data

models:
  my_duckdb_project:
    stg:
      +schema: stg
      +materialized: view
    int:
      +schema: int
    marts:
      +schema: marts
